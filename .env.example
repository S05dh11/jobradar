# 火山方舟 Agent Plan 专属 Key(控制台 → 开通管理 → API Key → Agent Plan 标签)
# 同一把 Key 同时用于:模型调用(/api/plan/v3)+ 豆包搜索(open.feedcoopapi.com)
# 每月 500 次免费搜索额度,每月 1 日重置
ARK_API_KEY=your-agent-plan-key

# 以下为可选覆盖项(默认值即可用)
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/plan/v3
# ARK_MODEL=doubao-seed-evolving(滚动别名,默认即可)

# 高德地图 Key(Web端 JS API 类型,控制台免费申请;
# 同一把 Key 需同时在「Web服务」品类下可用,供前端 geocode 反查公司坐标)
NEXT_PUBLIC_AMAP_KEY=your-amap-key
